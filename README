diary.sh is a shell script to keep a diary.

It automatically creates files (${DIARY_PREFIX}/<year>/<month>), inserts header
for each day, and open the file with the text editor (specified by the EDITOR
environment variable). If DIARY_PREFIX environment variable is not set, ~/diary
is used.
